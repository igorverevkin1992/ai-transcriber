# ABTGS - Broadcast Transcript Generation System

Frontend application for automated transcription and logging of broadcast materials using AI.

## 🚀 Запуск в облаке (Project IDX)

Вам не нужно ничего скачивать. Приложение работает прямо в браузере.

1.  **Дождитесь Rebuild Environment** (если среда предложила это сделать). Это установит `ffmpeg` и `python`.
2.  Откройте терминал (Terminal > New Terminal) и запустите Бэкенд:
    ```bash
    pip install -r requirements.txt
    python main.py
    ```
3.  Откройте **второй** терминал (кнопка `+` в панели терминала) и запустите Фронтенд:
    ```bash
    npm install
    npm run dev
    ```
4.  Нажмите на кнопку **"Project Previews"** или ссылку, которая появится, чтобы открыть приложение.

## 🚀 Запуск локально (на своем ПК)

### 1. Backend (Сервер Python)
Этот терминал должен оставаться открытым всегда.

1.  **Настройка ключей:**
    Откройте файл `.env` и убедитесь, что ваши ключи Yandex/AWS прописаны.
2.  **Установка библиотек:**
    ```bash
    pip install -r requirements.txt
    ```
    *(Если нет FFmpeg, установите его отдельно и добавьте в PATH)*
3.  **Запуск:**
    ```bash
    python main.py
    ```
    Сервер запустится на `http://localhost:8000`.

### 2. Frontend (Интерфейс)
Откройте **новый** терминал.

1.  **Установка:**
    ```bash
    npm install
    ```
2.  **Запуск:**
    ```bash
    npm start
    ```
    Интерфейс откроется на `http://localhost:3000`.

## ⚙️ Architecture

*   **Frontend**: React (TypeScript) - Handles UI, Speaker Verification, and Preview.
*   **Backend**: Python (FastAPI) - Handles downloading, FFmpeg conversion, and Yandex SpeechKit integration.
